#import "./postFragment.graphql"
#import "../collection/collectionFragment.graphql"

query postWithMoviesQuery {
    postWithMovies {
        ...postFragment
        collections {
            ...collectionFragment
        }
    }
}
